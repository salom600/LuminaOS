# LuminaOS — Live ISO initramfs feature set
# Used by mkinitfs to decide which modules/files to bake into initramfs.

features="ata base ide scsi usb virtio ext4 squashfs iso9660 vfat fs-uuid resume loop"
